version: "3.4"
services:
  broker:
    image: redis:7
    container_name: paperlessredis
    restart: unless-stopped
    security_opt:
        - no-new-privileges:true
    volumes:
      - /path/to/config:/data

  db:
    image: postgres:15
    container_name: paperlessdb
    restart: unless-stopped
    volumes:
      - /path/to/config:/var/lib/postgresql/data
    security_opt:
        - no-new-privileges:true
    environment:
      POSTGRES_DB: 
      POSTGRES_USER: 
      POSTGRES_PASSWORD: 

  webserver:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: paperlessngx
    restart: unless-stopped
    security_opt:
        - no-new-privileges:true
    ports:
      - 8800:8000
    volumes:
      - /path/to/config:/usr/src/paperless/data
      - /path/to/config:/usr/src/paperless/media
      - /path/to/config:/usr/src/paperless/export
      - /path/to/config:/usr/src/paperless/consume
    environment:
      PAPERLESS_REDIS: redis://broker:6379
      PAPERLESS_DBHOST: db
      USERMAP_UID:
      USERMAP_GID:
      TZ: London/Europe
      PAPERLESS_TIKA_ENABLED: 1
      PAPERLESS_ADMIN_USER: 
      PAPERLESS_ADMIN_PASSWORD: 
      PAPERLESS_TIKA_GOTENBERG_ENDPOINT: http://gotenberg:3000
      PAPERLESS_TIKA_ENDPOINT: http://tika:9998

  gotenberg:
    image: docker.io/gotenberg/gotenberg:7.10
    container_name: paperlessgotenberg
    security_opt:
        - no-new-privileges:true
    restart: unless-stopped
    ports:
      - 3001:3000

    command:
      - "gotenberg"
      - "--chromium-disable-javascript=true"
      - "--chromium-allow-list=file:///tmp/.*"

  tika:
    image: ghcr.io/paperless-ngx/tika:latest
    container_name: paperlesstika
    security_opt:
        - no-new-privileges:true
    restart: unless-stopped
    ports:
      - 9998:9998
